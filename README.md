# Screens

A historical view of screen sizes and resolutions.

## Setup

1. Install dependencies: `$ npm install`
2. Start a local mongodb
3. Setup `.env` file with connection string and auth server details (see `.env.example`)
4. Start dev server: `$ npm run start`

## Project files

Requirements: [Requirements](/requirements.md)

Documentation: [Documentation](/documentation.md)
